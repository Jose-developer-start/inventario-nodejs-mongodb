<div class="row">
    <div class="col-md-4 mx-auto">
        <div class="card">
            <div class="card-header">
                Ingresar
            </div>
            <div class="card-body">
                <form action="/users/signin" method="POST">
                    <div class="form-group">
                        <input name="email" type="email" class="form-control" value="{{email}}" placeholder="Email">
                    </div>
                    <div class="form-group">
                        <input name="password" type="password" value="{{password}}" class="form-control" placeholder="Password">
                    </div>
                    <button class="btn btn-secondary btn-block">Signin</button>
                </form>
            </div>
        </div>
    </div>
</div>